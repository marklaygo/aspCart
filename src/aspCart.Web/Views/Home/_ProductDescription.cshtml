@model ProductModel
<div class="container">
    <div class="col-md-12">
        @if (Model.Description != null)
        {
            @Html.Raw(Model.Description)
        }
        else
        {             
            <div>No description yet</div>
        }
    </div>
</div>
